import { StyleSheet } from "react-native"
import colors from "./colors"


const utilClass = StyleSheet.create({
    button: {
        width: '100%',
        padding: 12,
        backgroundColor: colors.primary,
        borderRadius: 5,
        alignItems: 'center',
    }
})

export default utilClass